[project]
name = "mm-community"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "google-generativeai",
    "python-multipart"
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

